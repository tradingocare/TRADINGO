# TRADINGO B2B Platform — Admin Flow UAT Test Cases

| Test ID | Description | Preconditions | Test Steps | Expected Result | Status |
|---------|-------------|---------------|------------|-----------------|--------|
| TC-A-01 | Admin login with MFA | Admin credentials exist; MFA app configured | 1. Navigate to `/admin/login`<br>2. Enter admin email and password<br>3. Enter TOTP from authenticator app<br>4. Click "Verify & Login" | Admin dashboard loads; session cookie set with httpOnly and Secure flags; MFA token expires after 30s; failed attempts lockout after 5 tries | |
| TC-A-02 | User management (view, suspend, verify) | Admin logged in; users exist in system | 1. Go to Admin → Users<br>2. View user list with filters (role, status, date range)<br>3. Search by email, phone, or company name<br>4. Click user to view full profile<br>5. Suspend a user — select reason, enter note<br>6. Reactivate suspended user<br>7. Verify email/phone manually | User list paginated; search returns results within 2s; suspension applied immediately; user receives suspension email; reactivation restores access | |
| TC-A-03 | Company verification (KYC approval/rejection) | Admin logged in; pending KYC submissions exist | 1. Go to Admin → KYC Requests<br>2. View pending requests sorted by date<br>3. Click request to view uploaded documents (inline preview)<br>4. Verify GSTIN via API check (click "Verify GST")<br>5. Approve KYC — enter notes<br>6. Reject another — select reason, add comment, request re-upload | GSTIN verification API returns valid/invalid; approved user notified with email; rejected user sees reason and can re-submit; dashboard KYC count updates | |
| TC-A-04 | Dispute resolution | Admin logged in; open disputes exist | 1. Go to Admin → Disputes<br>2. View open disputes list with priority (urgent/normal)<br>3. Click dispute → view chat log and evidence from both parties<br>4. Send message to both buyer and seller<br>5. Review evidence and proposed resolution<br>6. Rule in favor of buyer — initiate refund<br>7. Rule in favor of seller — release payment<br>8. Close dispute with final note | Dispute timeline shows all activity; party messages delivered; resolution action executed (refund/payment release); dispute status set to "Resolved"; both parties notified | |
| TC-A-05 | Payment monitoring | Admin logged in; transactions exist | 1. Go to Admin → Payments<br>2. View payment transactions list (date, user, amount, status, gateway ref)<br>3. Filter by status (success, failed, pending, refunded)<br>4. Click transaction to view full details<br>5. Manually trigger settlement for a held payment<br>6. View settlement report | Payments list loads within 3s; filters work; transaction details include Razorpay payment ID and webhook log; manual settlement triggers correctly; CSV export works | |
| TC-A-06 | Fraud detection dashboard | Admin logged in; platform has active users | 1. Go to Admin → Fraud Detection<br>2. View flagged transactions/users with risk score (0–100)<br>3. Click flagged item to see rules triggered (e.g., "Multiple accounts same IP", "Rapid RFQ creation")<br>4. Mark as "Reviewing"<br>5. Dismiss false positive<br>6. Confirm fraud — trigger auto-suspend | Dashboard loads with real-time data; risk scores calculated by rule engine; marking items updates status; confirmed fraud triggers user suspension; audit trail recorded | |
| TC-A-07 | System health monitoring | Admin logged in | 1. Go to Admin → System Health<br>2. View service status indicators (API, DB, Redis, OpenSearch, ClickHouse, CDN)<br>3. Check response times and uptime %<br>4. View error rate graph (last 24h)<br>5. Trigger a health check refresh<br>6. View recent alerts | All services show green/yellow/red status; response times within SLA; error rate graph renders; health check refresh updates timestamps; alerts show acknowledged/unacknowledged status | |
| TC-A-08 | Audit log review | Admin logged in; logs exist | 1. Go to Admin → Audit Logs<br>2. View paginated audit trail (timestamp, user, action, resource, IP)<br>3. Filter by action type (create, update, delete, login)<br>4. Filter by date range<br>5. Search by user email or resource ID<br>6. Export filtered results as CSV | Audit logs load with lazy loading; filters applied within 2s; CSV export contains all columns; search returns relevant entries; timestamps in IST | |
| TC-A-09 | Platform analytics | Admin logged in; platform data exists | 1. Go to Admin → Analytics<br>2. View key metrics cards (Total Users, Sellers, Buyers, Orders, GMV)<br>3. View user growth chart (line chart, 30d)<br>4. View order volume chart (bar chart)<br>5. View category-wise product distribution<br>6. View top sellers table (by GMV)<br>7. Apply date filter | All charts load with correct data; date range filter updates all components; tables sortable by columns; drill-down works (click category → details); data matches DB counts | |
| TC-A-10 | Configuration management | Admin logged in | 1. Go to Admin → Configuration<br>2. View config categories (platform fees, limits, features, notifications)<br>3. Update platform commission percentage (e.g., 2.5% → 3.0%)<br>4. Toggle feature flags (e.g., enable/disable TradGo)<br>5. Update RFQ expiry duration (e.g., 7 days → 14 days)<br>6. Click "Save Changes"<br>7. Verify config is immediately applied | Config saved with audit log entry (admin ID, old value, new value, timestamp); changes take effect without restart; UI reflects updated values; feature toggle hides/shows feature across platform | |
